# Changelog

All notable changes to `testing-skills` are documented here. Versioning follows [SemVer](https://semver.org/).

## 1.0.0

### Added
- Initial release. The `react-testing-library` skill, extracted from `frontend-skills` 1.0.0 so testing
  guidance can be installed on its own.

### Migration
- The skill's address changed from `frontend-skills:react-testing-library` to
  `testing-skills:react-testing-library`.
